<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>CineGen - AI Cinematic Video Generator</title>
  
  <%= csrf_meta_tags %>
  <%= csp_meta_tag %>
  <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
  <%= javascript_importmap_tags %>
  
  <style>
    * {
      margin: 0;
      padding: 0;
      box-sizing: border-box;
    }
    
    body {
      font-family: 'Segoe UI', 'Roboto', 'Arial', sans-serif;
      background: linear-gradient(135deg, #0f0f23 0%, #1a1a2e 50%, #16213e 100%);
      color: #ffffff;
      min-height: 100vh;
      overflow-x: hidden;
    }
    
    .hero-section {
      padding: 6rem 2rem 4rem;
      text-align: center;
      background: radial-gradient(ellipse at center, rgba(138, 43, 226, 0.15) 0%, rgba(0, 0, 0, 0.3) 70%);
      border-bottom: 2px solid #8a2be2;
      position: relative;
    }
    
    .hero-content {
      position: relative;
      z-index: 1;
    }
    
    .logo {
      font-size: 5rem;
      margin-bottom: 1rem;
      text-shadow: 0 0 30px #8a2be2;
    }
    
    .hero-title {
      font-size: 3.5rem;
      font-weight: 700;
      margin-bottom: 1rem;
      text-shadow: 0 0 20px #8a2be2;
      background: linear-gradient(45deg, #8a2be2, #ff6b6b, #4ecdc4);
      -webkit-background-clip: text;
      -webkit-text-fill-color: transparent;
      background-clip: text;
    }
    
    .hero-tagline {
      font-size: 1.5rem;
      color: #e0e0e0;
      margin-bottom: 2rem;
      font-weight: 300;
    }
    
    .hero-description {
      max-width: 700px;
      margin: 0 auto 3rem;
      color: #b8b8b8;
      line-height: 1.7;
      font-size: 1.1rem;
    }
    
    .stats-grid {
      display: grid;
      grid-template-columns: repeat(auto-fit, minmax(150px, 1fr));
      gap: 1.5rem;
      max-width: 600px;
      margin: 0 auto;
    }
    
    .stat-card {
      background: rgba(138, 43, 226, 0.1);
      border: 1px solid #8a2be2;
      border-radius: 12px;
      padding: 1.5rem;
      text-align: center;
      transition: all 0.3s ease;
    }
    
    .stat-value {
      font-size: 2rem;
      font-weight: bold;
      color: #8a2be2;
      display: block;
      margin-bottom: 0.5rem;
    }
    
    .stat-label {
      font-size: 0.9rem;
      color: #b8b8b8;
      text-transform: uppercase;
      letter-spacing: 1px;
    }
    
    .interface-section {
      padding: 4rem 2rem;
      max-width: 1400px;
      margin: 0 auto;
    }
    
    .section-header {
      text-align: center;
      margin-bottom: 3rem;
    }
    
    .section-title {
      font-size: 2.5rem;
      color: #8a2be2;
      margin-bottom: 1rem;
      font-weight: 600;
    }
    
    .section-subtitle {
      color: #b8b8b8;
      font-size: 1.1rem;
      max-width: 600px;
      margin: 0 auto;
    }
    
    .generator-container {
      background: rgba(255, 255, 255, 0.05);
      border: 1px solid rgba(138, 43, 226, 0.3);
      border-radius: 20px;
      padding: 3rem;
      box-shadow: 0 20px 40px rgba(0, 0, 0, 0.3);
      backdrop-filter: blur(10px);
    }
    
    .input-group {
      margin-bottom: 2rem;
    }
    
    .input-label {
      display: block;
      color: #8a2be2;
      font-weight: 600;
      margin-bottom: 0.75rem;
      font-size: 1.1rem;
    }
    
    .prompt-input {
      width: 100%;
      background: rgba(0, 0, 0, 0.4);
      border: 2px solid rgba(138, 43, 226, 0.3);
      border-radius: 12px;
      padding: 1.5rem;
      color: #ffffff;
      font-size: 1rem;
      font-family: inherit;
      resize: vertical;
      min-height: 120px;
      transition: all 0.3s ease;
    }
    
    .prompt-input:focus {
      outline: none;
      border-color: #8a2be2;
      box-shadow: 0 0 20px rgba(138, 43, 226, 0.4);
      background: rgba(0, 0, 0, 0.6);
    }
    
    .generate-button {
      width: 100%;
      background: linear-gradient(45deg, #8a2be2, #ff6b6b);
      border: none;
      border-radius: 12px;
      color: #ffffff;
      padding: 1.5rem 2rem;
      font-size: 1.2rem;
      font-weight: 600;
      cursor: pointer;
      transition: all 0.3s ease;
    }
    
    .generate-button:hover {
      transform: translateY(-2px);
      box-shadow: 0 10px 30px rgba(138, 43, 226, 0.4);
    }
    
    @media (max-width: 768px) {
      .hero-section {
        padding: 3rem 1rem 2rem;
      }
      
      .hero-title {
        font-size: 2.5rem;
      }
      
      .logo {
        font-size: 3rem;
      }
    }
  </style>
</head>

<body>
  <section class="hero-section">
    <div class="hero-content">
      <div class="logo">🎬</div>
      <h1 class="hero-title">CineGen</h1>
      <p class="hero-tagline">AI-Powered Cinematic Video Generation</p>
      <p class="hero-description">
        Transform your ideas into stunning cinematic videos using cutting-edge AI technology. 
        Create professional-quality content with just a text description.
      </p>
      
      <div class="stats-grid">
        <div class="stat-card">
          <span class="stat-value">10K+</span>
          <span class="stat-label">Videos Created</span>
        </div>
        <div class="stat-card">
          <span class="stat-value">4.9★</span>
          <span class="stat-label">User Rating</span>
        </div>
        <div class="stat-card">
          <span class="stat-value">2 Min</span>
          <span class="stat-label">Avg Generation</span>
        </div>
      </div>
    </div>
  </section>

  <section class="interface-section">
    <div class="section-header">
      <h2 class="section-title">🎥 Video Generator</h2>
      <p class="section-subtitle">
        Describe your vision and watch it come to life with AI-generated cinematic video
      </p>
    </div>
    
    <div class="generator-container">
      <form id="generation-form">
        <div class="input-group">
          <label for="prompt" class="input-label">📝 Video Description</label>
          <textarea 
            id="prompt" 
            name="prompt" 
            class="prompt-input"
            placeholder="Describe your video in detail... 

Example: 'A majestic dragon soaring through storm clouds above a medieval castle at sunset, with dramatic lighting and cinematic camera movements'"
            required
          ></textarea>
        </div>
        
        <button type="submit" class="generate-button" id="generate-btn">
          🎬 Generate Cinematic Video
        </button>
      </form>
    </div>
  </section>

  <script>
    document.addEventListener('DOMContentLoaded', () => {
      const form = document.getElementById('generation-form');
      const generateBtn = document.getElementById('generate-btn');
      
      form.addEventListener('submit', async (e) => {
        e.preventDefault();
        
        const prompt = document.getElementById('prompt').value.trim();
        if (!prompt) return;
        
        generateBtn.disabled = true;
        generateBtn.textContent = '🎬 Generating...';
        
        // Simulate generation for demo
        setTimeout(() => {
          alert('Demo mode: Video generation would happen here!');
          generateBtn.disabled = false;
          generateBtn.textContent = '🎬 Generate Cinematic Video';
        }, 2000);
      });
    });
  </script>
</body>
</html>

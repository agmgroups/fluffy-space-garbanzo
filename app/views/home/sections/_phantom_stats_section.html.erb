<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Phantom Stats</title>
  <script src="https://cdn.tailwindcss.com"></script>
  <style>
    /* Fog Layers */
    .fog-layer {
      position: absolute;
      width: 100%;
      height: 100%;
      background: radial-gradient(circle, rgba(255,255,255,0.03) 0%, transparent 70%);
      animation: fogMove 60s linear infinite;
    }
    .fog-1 { top: 0; left: 0; animation-delay: 0s; }
    .fog-2 { top: 0; right: 0; animation-delay: 10s; }
    .fog-3 { bottom: 0; left: 0; animation-delay: 20s; }

    @keyframes fogMove {
      0% { transform: translateX(0); }
      100% { transform: translateX(100px); }
    }

    /* Spectral Orbs */
    .spectral-orb {
      position: absolute;
      width: 40px;
      height: 40px;
      border-radius: 9999px;
      background: radial-gradient(circle, rgba(255,255,255,0.1), transparent);
      animation: floatOrb 12s ease-in-out infinite;
    }
    .orb-1 { top: 10%; left: 20%; animation-delay: 0s; }
    .orb-2 { top: 30%; right: 15%; animation-delay: 2s; }
    .orb-3 { bottom: 20%; left: 25%; animation-delay: 4s; }
    .orb-4 { bottom: 10%; right: 10%; animation-delay: 6s; }
    .orb-5 { top: 50%; left: 50%; animation-delay: 8s; }
    .orb-6 { top: 70%; right: 30%; animation-delay: 10s; }

    @keyframes floatOrb {
      0%, 100% { transform: translateY(0); opacity: 0.2; }
      50% { transform: translateY(-20px); opacity: 0.4; }
    }

    /* Number Counter */
    .phantom-number {
      transition: all 0.3s ease;
    }
  </style>
</head>
<body class="bg-black text-white">

  <!-- Phantom Dark Stats Section -->
  <section class="phantom-stats relative py-32 overflow-hidden">
    <!-- Fog and Shadow Background -->
    <div class="phantom-background absolute inset-0 z-0">
      <div class="fog-layer fog-1"></div>
      <div class="fog-layer fog-2"></div>
      <div class="fog-layer fog-3"></div>
    </div>

    <!-- Floating Spectral Particles -->
    <div class="spectral-particles absolute inset-0 pointer-events-none z-10">
      <div class="spectral-orb orb-1"></div>
      <div class="spectral-orb orb-2"></div>
      <div class="spectral-orb orb-3"></div>
      <div class="spectral-orb orb-4"></div>
      <div class="spectral-orb orb-5"></div>
      <div class="spectral-orb orb-6"></div>
    </div>

    <!-- Stats Content -->
    <div class="container mx-auto px-6 relative z-20">
      <div class="text-center mb-20">
        <h2 class="text-5xl lg:text-7xl font-bold mb-6 text-white drop-shadow-lg backdrop-blur-sm">
          <span class="inline-block bg-gradient-to-r from-purple-500 via-indigo-500 to-blue-500 bg-clip-text text-transparent animate-pulse">
            Into The Shadows
          </span>
        </h2>
        <p class="text-xl text-white/80 max-w-3xl mx-auto backdrop-blur-sm">
          Where darkness meets intelligence, our AI emerges from the void to serve enterprise needs
        </p>
      </div>

      <!-- Glassmorphism Stats Cards -->
      <div class="grid grid-cols-1 md:grid-cols-3 gap-12 max-w-6xl mx-auto">
        <!-- Card 1 -->
        <div class="bg-white/5 backdrop-blur-md border border-white/10 rounded-2xl p-8 text-center transition duration-300 hover:border-purple-500/50 hover:shadow-purple-500/30 hover:shadow-lg">
          <div class="text-4xl mb-4">👻</div>
          <div class="text-5xl font-bold text-white drop-shadow-md phantom-number" data-target="100">0</div>
          <div class="text-xl text-purple-400 font-semibold">K+</div>
          <div class="mt-2 text-lg text-white/90 font-medium">Souls Guided</div>
          <p class="mt-4 text-sm text-white/60">Lost developers found their path through our AI darkness</p>
        </div>

        <!-- Card 2 -->
        <div class="bg-white/5 backdrop-blur-md border border-white/10 rounded-2xl p-8 text-center transition duration-300 hover:border-indigo-500/50 hover:shadow-indigo-500/30 hover:shadow-lg">
          <div class="text-4xl mb-4">🌑</div>
          <div class="text-5xl font-bold text-white drop-shadow-md phantom-number" data-target="50">0</div>
          <div class="text-xl text-indigo-400 font-semibold"></div>
          <div class="mt-2 text-lg text-white/90 font-medium">Realms Conquered</div>
          <p class="mt-4 text-sm text-white/60">Countries where our shadow intelligence has spread</p>
        </div>

        <!-- Card 3 -->
        <div class="bg-white/5 backdrop-blur-md border border-white/10 rounded-2xl p-8 text-center transition duration-300 hover:border-blue-500/50 hover:shadow-blue-500/30 hover:shadow-lg">
          <div class="text-4xl mb-4">⚡</div>
          <div class="text-5xl font-bold text-white drop-shadow-md phantom-number" data-target="99.9">0</div>
          <div class="text-xl text-blue-400 font-semibold">%</div>
          <div class="mt-2 text-lg text-white/90 font-medium">Eternal Vigilance</div>
          <p class="mt-4 text-sm text-white/60">Uptime that never sleeps, watching from the shadows</p>
        </div>
      </div>
    </div>
  </section>

  <!-- Counter Script -->
  <script>
    document.querySelectorAll('.phantom-number').forEach(el => {
      const target = parseFloat(el.getAttribute('data-target'));
      let count = 0;
      const increment = target / 100;
      const update = () => {
        count += increment;
        if (count >= target) {
          el.textContent = target;
        } else {
          el.textContent = Math.round(count * 10) / 10;
          requestAnimationFrame(update);
        }
      };
      update();
    });
  </script>
</body>
</html>

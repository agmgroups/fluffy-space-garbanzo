<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Elite Pricing Plans</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <style>
        @import url('https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700;800;900&display=swap');
        body {
            font-family: 'Inter', sans-serif;
            background-color: #0A0A0A; /* Deep matte black background */
            color: #e0e7ff;
            overflow-x: hidden;
        }

        /* --- Canvas Background (for interactive elements) --- */
        #interactive-canvas {
            position: absolute;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            z-index: 1;
        }

        /* --- Pricing Section --- */
        .elite-pricing-section {
            position: relative;
            z-index: 2;
        }

        /* --- Glassmorphism Card Effect & Shadow Layer (The 'Fog') --- */
        .glass-card {
            backdrop-filter: blur(16px) saturate(180%);
            -webkit-backdrop-filter: blur(16px) saturate(180%);
            border: 2px solid rgba(147, 51, 234, 0.5); /* Purple border */
            border-radius: 1.5rem; /* Soft rounded corners */
            box-shadow: 0 0 10px rgba(147, 51, 234, 0.4); /* Purple glow */
            transition: transform 0.3s ease-in-out, box-shadow 0.3s ease-in-out;
            position: relative;
            z-index: 3;
        }

        .glass-card:hover {
            transform: translateY(-8px) scale(1.02);
            box-shadow: 0 0 25px rgba(147, 51, 234, 0.8), 0 0 40px rgba(147, 51, 234, 0.6); /* Enhanced purple glow */
        }
        
        /* --- Card Specific Hover States --- */
        .glass-card.best-offer-card:hover {
            box-shadow: 0 0 25px rgba(147, 51, 234, 0.8), 0 0 40px rgba(255, 215, 0, 0.5);
        }

        .glass-card.featured-card:hover {
            box-shadow: 0 0 25px rgba(147, 51, 234, 0.8), 0 0 40px rgba(236, 72, 153, 0.5);
        }

        .glass-card.ultimate-card:hover {
            box-shadow: 0 0 25px rgba(147, 51, 234, 0.8), 0 0 40px rgba(255, 165, 0, 0.5);
        }

        /* --- Button Glow Effects --- */
        .glow-button {
            box-shadow: 0 0 20px rgba(109, 40, 217, 0.5);
            transition: background-color 0.3s ease-in-out, box-shadow 0.3s ease-in-out;
        }

        .glow-button:hover {
            background-color: #4c1d95;
            box-shadow: 0 0 25px rgba(109, 40, 217, 0.8);
        }

        .featured-card .glow-button {
            background-color: #3b82f6;
            box-shadow: 0 0 20px rgba(59, 130, 246, 0.5);
        }
        .featured-card .glow-button:hover {
            background-color: #2563eb;
            box-shadow: 0 0 25px rgba(59, 130, 246, 0.8);
        }

        .ultimate-card .glow-button {
            background-color: #f97316;
            box-shadow: 0 0 20px rgba(249, 115, 22, 0.5);
        }
        .ultimate-card .glow-button:hover {
            background-color: #ea580c;
            box-shadow: 0 0 25px rgba(249, 115, 22, 0.8);
        }

        .best-offer-card .glow-button {
            background-color: #10b981;
            box-shadow: 0 0 20px rgba(16, 185, 129, 0.5);
        }
        .best-offer-card .glow-button:hover {
            background-color: #059669;
            box-shadow: 0 0 25px rgba(16, 185, 129, 0.8);
        }

        @keyframes pulse-glow {
            0%, 100% { box-shadow: 0 0 8px rgba(255, 255, 255, 0.3); }
            50% { box-shadow: 0 0 20px rgba(255, 255, 255, 0.7); }
        }

        .animated-label {
            animation: pulse-glow 2s infinite ease-in-out;
        }

        /* Responsive Layout for Vertical Stack on Mobile */
        .pricing-grid-container {
            /* This is a grid for larger screens and handles the vertical stack on smaller screens */
            display: grid;
            grid-template-columns: 1fr;
            gap: 2rem;
        }

        @media (min-width: 768px) {
            .pricing-grid-container {
                grid-template-columns: repeat(2, 1fr);
            }
        }

        @media (min-width: 1024px) {
            .pricing-grid-container {
                grid-template-columns: repeat(4, 1fr);
            }
        }

        .price-highlight {
            color: #E0E7FF;
        }
    </style>
</head>
<body class="bg-[#0A0A0A] text-white min-h-screen">

    <!-- Canvas Background -->
    <canvas id="interactive-canvas"></canvas>

    <!-- Pricing Plans Section -->
    <section class="elite-pricing-section relative py-24 z-20">
        <div class="container mx-auto px-6 relative z-20">
            <div class="text-center mb-16">
                <h2 class="text-5xl lg:text-6xl font-extrabold mb-6 text-transparent bg-clip-text bg-gradient-to-r from-purple-400 via-pink-400 to-indigo-400">
                    Elite Pricing Plans
                </h2>
                <p class="text-xl text-gray-300 max-w-3xl mx-auto">Choose your path to mastery with these exclusive crystal-tier plans.</p>
            </div>

            <!-- Pricing Grid -->
            <div class="flex justify-center">
                <div class="pricing-grid-container">
                
                    <!-- Weekly Plan - Glass Card -->
                    <div class="glass-card flex flex-col p-6 rounded-3xl shadow-lg border-2 border-gray-700/50 hover:border-purple-500/50 transition-all duration-300 bg-gray-900/50 text-gray-200">
                        <div class="flex items-center justify-between mb-2">
                            <div class="bg-gray-300/50 text-gray-700 px-3 py-1 rounded-full text-sm font-semibold">Quick Start</div>
                            <div class="w-10 h-10 rounded-full bg-gradient-to-tr from-cyan-500 to-blue-500 flex items-center justify-center">
                                <span class="text-white text-lg">🌌</span>
                            </div>
                        </div>
                        
                        <h3 class="text-2xl font-bold mb-2">Weekly Access</h3>
                        <p class="text-gray-400 text-sm italic mb-4">Perfect for: Quick trials & new explorers</p>
                        <p class="text-gray-400 mb-4 flex-grow">Perfect for trying out our AI Agents.</p>
                        
                        <div class="mb-6">
                            <div class="flex items-baseline">
                                <span class="text-4xl font-extrabold price-highlight">$3</span>
                                <span class="text-gray-400 ml-2">/week</span>
                            </div>
                            <span class="text-xs block mt-1" style="color: #757575;">(ex. tax)</span>
                        </div>
                        
                        <ul class="space-y-3 text-gray-400 mb-6">
                            <li><span class="text-gray-300 mr-2">✓</span> 1 AI Agent Access</li>
                            <li><span class="text-gray-300 mr-2">✓</span> Unlimited Conversations</li>
                            <li><span class="text-gray-300 mr-2">✓</span> Mobile & Web Access</li>
                            <li><span class="text-gray-300 mr-2">✓</span> Real-time Responses</li>
                        </ul>
                        
                        <button class="w-full py-3 px-6 rounded-full text-white font-semibold text-lg transition-all duration-300 glow-button bg-gray-800 hover:bg-gray-700">Get Started Now</button>
                    </div>

                    <!-- Monthly Plan - Glass Card (Featured) -->
                    <div class="glass-card featured-card flex flex-col p-6 rounded-3xl shadow-lg border-2 border-purple-500/50 hover:border-pink-500/50 transition-all duration-300 bg-gray-900/50 text-gray-200">
                        <div class="absolute -top-3 left-1/2 transform -translate-x-1/2">
                            <span class="bg-gradient-to-r from-purple-600 to-pink-600 text-white text-sm px-4 py-1.5 rounded-full font-bold uppercase tracking-wider animated-label">Most Popular</span>
                        </div>
                        
                        <div class="flex items-center justify-between mb-2 mt-4">
                            <div class="bg-gray-300/50 text-gray-700 px-3 py-1 rounded-full text-sm font-semibold">Premium</div>
                            <div class="w-10 h-10 rounded-full bg-gradient-to-tr from-purple-500 to-pink-500 flex items-center justify-center">
                                <span class="text-white text-lg">🚀</span>
                            </div>
                        </div>
                        
                        <h3 class="text-2xl font-bold mb-2">Monthly Access</h3>
                        <p class="text-gray-400 text-sm italic mb-4">Perfect for: Dedicated creators & teams</p>
                        <p class="text-gray-400 mb-2 flex-grow">Ideal for regular AI assistance.</p>
                        <div class="text-sm text-gray-400 mb-4">Save 23% vs Weekly!</div>
                        
                        <div class="mb-6">
                            <div class="flex items-baseline">
                                <span class="text-4xl font-extrabold price-highlight">$10</span>
                                <span class="text-gray-400 ml-2">/month</span>
                            </div>
                            <span class="text-xs block mt-1" style="color: #757575;">(ex. tax)</span>
                        </div>
                        
                        <ul class="space-y-3 text-gray-400 mb-6">
                            <li><span class="text-gray-300 mr-2">✓</span> 1 AI Agent Access</li>
                            <li><span class="text-gray-300 mr-2">✓</span> Priority Response Speed</li>
                            <li><span class="text-gray-300 mr-2">✓</span> Extended Chat History</li>
                            <li><span class="text-gray-300 mr-2">✓</span> Advanced Features</li>
                            <li><span class="text-gray-300 mr-2">✓</span> Usage Analytics</li>
                        </ul>
                        
                        <button class="w-full py-3 px-6 rounded-full text-white font-semibold text-lg transition-all duration-300 glow-button bg-indigo-800 hover:bg-indigo-700">Subscribe Now</button>
                    </div>

                    <!-- Yearly Plan - Glass Card (Best Offer) -->
                    <div class="glass-card best-offer-card flex flex-col p-6 rounded-3xl shadow-lg border-2 border-gray-700/50 hover:border-amber-500/50 transition-all duration-300 bg-gray-900/50 text-gray-200">
                        <div class="absolute -top-3 left-1/2 transform -translate-x-1/2">
                            <span class="bg-[#FFD700] text-[#333333] text-sm px-4 py-1.5 rounded-full font-bold uppercase tracking-wider animated-label">Best Offer</span>
                        </div>

                        <div class="flex items-center justify-between mb-2 mt-4">
                            <div class="bg-gray-300/50 text-gray-700 px-3 py-1 rounded-full text-sm font-semibold">Best Value</div>
                            <div class="w-10 h-10 rounded-full bg-gradient-to-tr from-amber-500 to-yellow-500 flex items-center justify-center">
                                <span class="text-white text-lg">🏆</span>
                            </div>
                        </div>
                        
                        <h3 class="text-2xl font-bold mb-2">Yearly Access</h3>
                        <p class="text-gray-400 text-sm italic mb-4">Perfect for: Power users & businesses</p>
                        <p class="text-gray-400 mb-2 flex-grow">Maximum value for dedicated users.</p>
                        <div class="text-sm text-gray-400 mb-4">Save 17% vs Monthly!</div>
                        
                        <div class="mb-6">
                            <div class="flex items-baseline">
                                <span class="text-4xl font-extrabold price-highlight">$99</span>
                                <span class="text-gray-400 ml-2">/year</span>
                            </div>
                            <span class="text-xs block mt-1" style="color: #757575;">(ex. tax)</span>
                        </div>
                        
                        <ul class="space-y-3 text-gray-400 mb-6">
                            <li><span class="text-gray-300 mr-2">✓</span> 1 AI Agent Access</li>
                            <li><span class="text-gray-300 mr-2">✓</span> Premium Features</li>
                            <li><span class="text-gray-300 mr-2">✓</span> Priority Support</li>
                            <li><span class="text-gray-300 mr-2">✓</span> Enhanced Security</li>
                            <li><span class="text-gray-300 mr-2">✓</span> Custom Integrations</li>
                        </ul>
                        
                        <button class="w-full py-3 px-6 rounded-full text-white font-semibold text-lg transition-all duration-300 glow-button bg-teal-600 hover:bg-teal-500">Join Yearly</button>
                    </div>

                    <!-- Lifetime Plan - Glass Card (Ultimate) -->
                    <div class="glass-card ultimate-card flex flex-col p-6 rounded-3xl shadow-lg border-2 border-pink-500/50 hover:border-orange-500/50 transition-all duration-300 bg-gray-900/50 text-gray-200">
                        <div class="absolute -top-3 left-1/2 transform -translate-x-1/2">
                            <span class="bg-gradient-to-r from-pink-600 to-purple-600 text-white text-sm px-4 py-1.5 rounded-full font-bold uppercase tracking-wider animated-label">Ultimate Deal</span>
                        </div>
                        
                        <div class="flex items-center justify-between mb-2 mt-4">
                            <div class="bg-gray-300/50 text-gray-700 px-3 py-1 rounded-full text-sm font-semibold">Ultimate</div>
                            <div class="w-10 h-10 rounded-full bg-gradient-to-tr from-pink-500 to-purple-500 flex items-center justify-center">
                                <span class="text-white text-lg">♾️</span>
                            </div>
                        </div>
                        
                        <h3 class="text-2xl font-bold mb-2">Lifetime Access</h3>
                        <p class="text-gray-400 text-sm italic mb-4">Designed for: Elite agents & organizations</p>
                        <p class="text-gray-400 mb-2 flex-grow">One payment, unlimited AI access forever.</p>
                        <div class="text-sm text-gray-400 mb-4">🎉 79% OFF vs Annual!</div>
                        
                        <div class="mb-6">
                            <div class="flex items-baseline">
                                <span class="text-4xl font-extrabold price-highlight">$249</span>
                                <span class="ml-2 text-pink-400">/forever</span>
                            </div>
                            <span class="text-xs block mt-1" style="color: #757575;">(ex. tax)</span>
                        </div>
                        
                        <ul class="space-y-3 text-gray-400 mb-6">
                            <li><span class="text-gray-300 mr-2">✓</span> 1 AI Agent Forever</li>
                            <li><span class="text-gray-300 mr-2">✓</span> Premium Everything</li>
                            <li><span class="text-gray-300 mr-2">✓</span> VIP Support Channel</li>
                            <li><span class="text-gray-300 mr-2">✓</span> Enterprise Security</li>
                            <li><span class="text-gray-300 mr-2">✓</span> Lifetime Updates</li>
                        </ul>
                        
                        <button class="w-full py-3 px-6 rounded-full text-white font-semibold text-lg transition-all duration-300 glow-button bg-orange-600 hover:bg-orange-500 mb-2">Unlock Forever</button>
                        <div class="text-center">
                            <span class="text-orange-400 text-xs font-semibold animate-pulse">⏰ Limited time offer!</span>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </section>

    <script>
        window.onload = function() {
            const canvas = document.getElementById('interactive-canvas');
            const ctx = canvas.getContext('2d');
            let mouseX = 0;
            let mouseY = 0;
            let particles = [];
            let particleCount = 100;
            const particleSize = 1.5;
            const particleSpeed = 0.5;

            // Set canvas size
            function resizeCanvas() {
                canvas.width = window.innerWidth;
                canvas.height = window.innerHeight;
            }

            resizeCanvas();
            window.addEventListener('resize', resizeCanvas);

            // Mouse interaction
            document.addEventListener('mousemove', (e) => {
                mouseX = e.clientX;
                mouseY = e.clientY;
            });

            // Particle class
            class Particle {
                constructor() {
                    this.x = Math.random() * canvas.width;
                    this.y = Math.random() * canvas.height;
                    this.vx = (Math.random() - 0.5) * particleSpeed;
                    this.vy = (Math.random() - 0.5) * particleSpeed;
                    this.size = particleSize;
                    this.color = `rgba(255, 255, 255, ${Math.random() * 0.5 + 0.2})`;
                }

                update() {
                    let dx = this.x - mouseX;
                    let dy = this.y - mouseY;
                    let distance = Math.sqrt(dx * dx + dy * dy);

                    if (distance < 150) {
                        let force = (150 - distance) / 150;
                        this.x += dx * force * 0.01;
                        this.y += dy * force * 0.01;
                    }

                    this.x += this.vx;
                    this.y += this.vy;

                    // Wrap around
                    if (this.x < 0) this.x = canvas.width;
                    if (this.x > canvas.width) this.x = 0;
                    if (this.y < 0) this.y = canvas.height;
                    if (this.y > canvas.height) this.y = 0;
                }

                draw() {
                    ctx.beginPath();
                    ctx.arc(this.x, this.y, this.size, 0, Math.PI * 2);
                    ctx.fillStyle = this.color;
                    ctx.fill();
                }
            }

            // Initialize particles
            function init() {
                particles = [];
                for (let i = 0; i < particleCount; i++) {
                    particles.push(new Particle());
                }
            }

            // Animation loop
            function animate() {
                ctx.fillStyle = 'rgba(10, 10, 10, 0.1)';
                ctx.fillRect(0, 0, canvas.width, canvas.height);

                for (let i = 0; i < particles.length; i++) {
                    let p1 = particles[i];
                    p1.update();
                    p1.draw();

                    // Connect particles
                    for (let j = i + 1; j < particles.length; j++) {
                        let p2 = particles[j];
                        let dx = p1.x - p2.x;
                        let dy = p1.y - p2.y;
                        let distance = Math.sqrt(dx * dx + dy * dy);

                        if (distance < 100) {
                            ctx.beginPath();
                            ctx.moveTo(p1.x, p1.y);
                            ctx.lineTo(p2.x, p2.y);
                            ctx.strokeStyle = `rgba(255, 255, 255, ${1 - distance / 100})`;
                            ctx.lineWidth = 0.5;
                            ctx.stroke();
                        }
                    }
                }
                requestAnimationFrame(animate);
            }

            init();
            animate();
        };
    </script>
</body>
</html>

<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Production Integrations & Deployment Center - OneLastAI</title>

  <%= csrf_meta_tags %>
  <%= csp_meta_tag %>
  <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
  <%= javascript_importmap_tags %>

  <style>
    * {
      margin: 0;
      padding: 0;
      box-sizing: border-box;
    }

    body {
      font-family: 'Segoe UI', 'Roboto', 'Arial', sans-serif;
      background: linear-gradient(135deg, #0f0f23 0%, #1a1a2e 50%, #16213e 100%);
      color: #ffffff;
      min-height: 100vh;
      overflow-x: hidden;
    }

    .hero-section {
      padding: 4rem 2rem 3rem;
      text-align: center;
      background: radial-gradient(ellipse at center, rgba(0, 212, 255, 0.15) 0%, rgba(0, 0, 0, 0.3) 70%);
      border-bottom: 2px solid #00d4ff;
    }

    .hero-title {
      font-size: 3rem;
      font-weight: 700;
      margin-bottom: 1rem;
      text-shadow: 0 0 20px #00d4ff;
      background: linear-gradient(45deg, #00d4ff, #0099cc, #66d9ff);
      -webkit-background-clip: text;
      -webkit-text-fill-color: transparent;
      background-clip: text;
    }

    .hero-icon {
      font-size: 3rem;
      margin-bottom: 1rem;
      text-shadow: 0 0 30px #00d4ff;
    }

    .hero-tagline {
      font-size: 1.3rem;
      color: #e0e0e0;
      margin-bottom: 1rem;
      font-weight: 300;
    }

    .status-bar {
      display: inline-flex;
      align-items: center;
      background: rgba(0, 255, 0, 0.1);
      border: 1px solid #00ff00;
      border-radius: 25px;
      padding: 0.5rem 1rem;
      margin-top: 1rem;
    }

    .status-dot {
      width: 8px;
      height: 8px;
      background: #00ff00;
      border-radius: 50%;
      margin-right: 0.5rem;
      animation: pulse 2s infinite;
    }

    @keyframes pulse {
      0%, 100% { opacity: 1; }
      50% { opacity: 0.5; }
    }

    .content-section {
      padding: 3rem 2rem;
      max-width: 1400px;
      margin: 0 auto;
    }

    .integration-grid {
      display: grid;
      grid-template-columns: repeat(auto-fit, minmax(400px, 1fr));
      gap: 2rem;
      margin-bottom: 3rem;
    }

    .integration-card {
      background: rgba(255, 255, 255, 0.05);
      border: 1px solid rgba(0, 212, 255, 0.2);
      border-radius: 16px;
      padding: 2rem;
      backdrop-filter: blur(10px);
      transition: all 0.3s ease;
    }

    .integration-card:hover {
      border-color: #00d4ff;
      transform: translateY(-5px);
      box-shadow: 0 10px 30px rgba(0, 212, 255, 0.2);
    }

    .card-header {
      display: flex;
      align-items: center;
      margin-bottom: 1.5rem;
    }

    .card-icon {
      font-size: 2.5rem;
      margin-right: 1rem;
    }

    .card-title {
      font-size: 1.5rem;
      font-weight: 600;
      color: #00d4ff;
      margin-bottom: 0.3rem;
    }

    .card-subtitle {
      color: #b8b8b8;
      font-size: 0.9rem;
    }

    .card-content {
      margin-bottom: 1.5rem;
    }

    .card-description {
      color: #e0e0e0;
      line-height: 1.6;
      margin-bottom: 1rem;
    }

    .feature-list {
      list-style: none;
    }

    .feature-list li {
      color: #b8b8b8;
      margin-bottom: 0.5rem;
      padding-left: 1.5rem;
      position: relative;
    }

    .feature-list li::before {
      content: "✅";
      position: absolute;
      left: 0;
    }

    .button-group {
      display: flex;
      gap: 1rem;
      flex-wrap: wrap;
    }

    .btn {
      padding: 0.8rem 1.5rem;
      border-radius: 10px;
      text-decoration: none;
      font-weight: 600;
      text-align: center;
      transition: all 0.3s ease;
      border: none;
      cursor: pointer;
      font-size: 0.9rem;
    }

    .btn-primary {
      background: linear-gradient(45deg, #00d4ff, #0099cc);
      color: #ffffff;
    }

    .btn-primary:hover {
      transform: translateY(-2px);
      box-shadow: 0 8px 25px rgba(0, 212, 255, 0.4);
    }

    .btn-secondary {
      background: rgba(255, 255, 255, 0.1);
      color: #e0e0e0;
      border: 1px solid rgba(255, 255, 255, 0.2);
    }

    .btn-secondary:hover {
      background: rgba(255, 255, 255, 0.2);
      transform: translateY(-2px);
    }

    .btn-success {
      background: linear-gradient(45deg, #00ff88, #00cc66);
      color: #ffffff;
    }

    .btn-warning {
      background: linear-gradient(45deg, #ffaa00, #ff8800);
      color: #ffffff;
    }

    .deployment-status {
      background: rgba(0, 255, 0, 0.1);
      border: 1px solid #00ff00;
      border-radius: 16px;
      padding: 2rem;
      margin-bottom: 3rem;
      text-align: center;
    }

    .deployment-status h2 {
      color: #00ff00;
      margin-bottom: 1rem;
    }

    .realtime-terminal {
      background: #1e1e1e;
      border: 1px solid #333;
      border-radius: 12px;
      padding: 1.5rem;
      font-family: 'Consolas', 'Monaco', 'Courier New', monospace;
      font-size: 0.9rem;
      line-height: 1.4;
      margin-top: 1rem;
      max-height: 200px;
      overflow-y: auto;
    }

    .terminal-line {
      margin-bottom: 0.3rem;
    }

    .terminal-prompt {
      color: #00ff00;
    }

    .terminal-output {
      color: #ffffff;
    }

    .terminal-error {
      color: #ff6b6b;
    }

    .terminal-warning {
      color: #ffaa00;
    }

    .back-button {
      display: inline-block;
      background: linear-gradient(45deg, #666, #888);
      color: #ffffff;
      padding: 1rem 2rem;
      border-radius: 12px;
      text-decoration: none;
      font-weight: 600;
      transition: all 0.3s ease;
      margin-top: 2rem;
    }

    .back-button:hover {
      transform: translateY(-2px);
      box-shadow: 0 10px 30px rgba(136, 136, 136, 0.4);
    }

    .progress-bar {
      width: 100%;
      height: 8px;
      background: rgba(255, 255, 255, 0.1);
      border-radius: 4px;
      overflow: hidden;
      margin: 1rem 0;
    }

    .progress-fill {
      height: 100%;
      background: linear-gradient(90deg, #00d4ff, #00ff88);
      transition: width 0.3s ease;
      animation: shimmer 2s infinite;
    }

    @keyframes shimmer {
      0% { transform: translateX(-100%); }
      100% { transform: translateX(100%); }
    }
  </style>
</head>

<body>
  <section class="hero-section">
    <div class="hero-icon">🚀</div>
    <h1 class="hero-title">Production Deployment Center</h1>
    <p class="hero-tagline">Real-time integrations, deployment management, and production monitoring</p>
    <div class="status-bar">
      <div class="status-dot"></div>
      <span>Production Ready • Live Deployment Active</span>
    </div>
  </section>

  <section class="content-section">
    <div class="deployment-status">
      <h2>🌐 Current Deployment Status</h2>
      <p style="color: #e0e0e0; margin-bottom: 1rem;">All systems operational • Last deployment: <span id="last-deployment">Just now</span></p>
      <div class="progress-bar">
        <div class="progress-fill" style="width: 100%"></div>
      </div>
      <div class="realtime-terminal" id="deployment-terminal">
        <div class="terminal-line"><span class="terminal-prompt">oneLastAI@production:~$</span> <span class="terminal-output">Deployment Status Check</span></div>
        <div class="terminal-line"><span class="terminal-output">✅ Rails Server: Running (PID: 1403)</span></div>
        <div class="terminal-line"><span class="terminal-output">✅ Database: Connected (MongoDB)</span></div>
        <div class="terminal-line"><span class="terminal-output">✅ AI Agents: 25+ Active</span></div>
        <div class="terminal-line"><span class="terminal-output">✅ Codespace: Live</span></div>
        <div class="terminal-line"><span class="terminal-output">✅ SSL: Secured</span></div>
        <div class="terminal-line"><span class="terminal-prompt">oneLastAI@production:~$</span> <span style="animation: blink 1s infinite;">_</span></div>
      </div>
    </div>

    <div class="integration-grid">
      <!-- GitHub Integration -->
      <div class="integration-card">
        <div class="card-header">
          <div class="card-icon">🐙</div>
          <div>
            <div class="card-title">GitHub Codespaces</div>
            <div class="card-subtitle">Development Environment</div>
          </div>
        </div>
        <div class="card-content">
          <p class="card-description">
            Fully integrated development environment with real-time collaboration and deployment capabilities.
          </p>
          <ul class="feature-list">
            <li>Live development server</li>
            <li>Automated Git workflows</li>
            <li>Real-time code sync</li>
            <li>Container-based deployment</li>
          </ul>
        </div>
        <div class="button-group">
          <a href="https://github.com/agmgroups/fluffy-space-garbanzo" class="btn btn-success">✅ Active</a>
          <a href="#" class="btn btn-secondary">Manage</a>
        </div>
      </div>

      <!-- Cloud Deployment -->
      <div class="integration-card">
        <div class="card-header">
          <div class="card-icon">☁️</div>
          <div>
            <div class="card-title">Cloud Deployment</div>
            <div class="card-subtitle">Production Infrastructure</div>
          </div>
        </div>
        <div class="card-content">
          <p class="card-description">
            Enterprise-grade cloud deployment with auto-scaling, load balancing, and global CDN distribution.
          </p>
          <ul class="feature-list">
            <li>Auto-scaling infrastructure</li>
            <li>Global load balancing</li>
            <li>CDN acceleration</li>
            <li>99.9% uptime SLA</li>
          </ul>
        </div>
        <div class="button-group">
          <a href="#" class="btn btn-primary">Deploy Now</a>
          <a href="#" class="btn btn-secondary">Configure</a>
        </div>
      </div>

      <!-- Database Integration -->
      <div class="integration-card">
        <div class="card-header">
          <div class="card-icon">🗄️</div>
          <div>
            <div class="card-title">MongoDB Atlas</div>
            <div class="card-subtitle">Cloud Database</div>
          </div>
        </div>
        <div class="card-content">
          <p class="card-description">
            Managed MongoDB database with automatic backups, scaling, and global replication.
          </p>
          <ul class="feature-list">
            <li>Automatic scaling</li>
            <li>Real-time backups</li>
            <li>Global clusters</li>
            <li>Advanced security</li>
          </ul>
        </div>
        <div class="button-group">
          <a href="#" class="btn btn-warning">Configure</a>
          <a href="#" class="btn btn-secondary">Monitor</a>
        </div>
      </div>

      <!-- AI Services -->
      <div class="integration-card">
        <div class="card-header">
          <div class="card-icon">🤖</div>
          <div>
            <div class="card-title">AI Service Providers</div>
            <div class="card-subtitle">OpenAI, Claude, Gemini</div>
          </div>
        </div>
        <div class="card-content">
          <p class="card-description">
            Integrated AI model providers with failover support and intelligent routing.
          </p>
          <ul class="feature-list">
            <li>Multi-provider support</li>
            <li>Automatic failover</li>
            <li>Cost optimization</li>
            <li>Usage analytics</li>
          </ul>
        </div>
        <div class="button-group">
          <a href="#" class="btn btn-primary">Setup Keys</a>
          <a href="#" class="btn btn-secondary">Analytics</a>
        </div>
      </div>

      <!-- Monitoring & Analytics -->
      <div class="integration-card">
        <div class="card-header">
          <div class="card-icon">📊</div>
          <div>
            <div class="card-title">Monitoring & Analytics</div>
            <div class="card-subtitle">Real-time Insights</div>
          </div>
        </div>
        <div class="card-content">
          <p class="card-description">
            Comprehensive monitoring with real-time analytics, error tracking, and performance metrics.
          </p>
          <ul class="feature-list">
            <li>Real-time monitoring</li>
            <li>Error tracking</li>
            <li>Performance metrics</li>
            <li>Custom dashboards</li>
          </ul>
        </div>
        <div class="button-group">
          <a href="#" class="btn btn-primary">View Dashboard</a>
          <a href="#" class="btn btn-secondary">Configure</a>
        </div>
      </div>

      <!-- Security & Compliance -->
      <div class="integration-card">
        <div class="card-header">
          <div class="card-icon">🔒</div>
          <div>
            <div class="card-title">Security & Compliance</div>
            <div class="card-subtitle">Enterprise Security</div>
          </div>
        </div>
        <div class="card-content">
          <p class="card-description">
            Enterprise-grade security with SSL/TLS, WAF protection, and compliance monitoring.
          </p>
          <ul class="feature-list">
            <li>SSL/TLS encryption</li>
            <li>WAF protection</li>
            <li>GDPR compliance</li>
            <li>Security auditing</li>
          </ul>
        </div>
        <div class="button-group">
          <a href="#" class="btn btn-success">✅ Secured</a>
          <a href="#" class="btn btn-secondary">Audit</a>
        </div>
      </div>
    </div>

    <div style="text-align: center;">
      <a href="/" class="back-button">← Back to Home</a>
    </div>
  </section>

  <script>
    // Real-time deployment status updates
    function updateDeploymentStatus() {
      const terminal = document.getElementById('deployment-terminal');
      const lastDeployment = document.getElementById('last-deployment');
      
      // Update last deployment time
      const now = new Date();
      lastDeployment.textContent = now.toLocaleTimeString();
      
      // Simulate real-time terminal updates
      const messages = [
        '✅ Health check: All systems operational',
        '📊 Memory usage: 45% (Normal)',
        '🔄 Auto-scaling: Ready',
        '🌐 CDN: Global distribution active',
        '🛡️ Security scan: No threats detected'
      ];
      
      const randomMessage = messages[Math.floor(Math.random() * messages.length)];
      const newLine = document.createElement('div');
      newLine.className = 'terminal-line';
      newLine.innerHTML = `<span class="terminal-output">${randomMessage}</span>`;
      
      // Add new line and remove old ones to keep terminal clean
      const lines = terminal.querySelectorAll('.terminal-line');
      if (lines.length > 8) {
        lines[1].remove(); // Keep first line (prompt)
      }
      
      // Insert before the cursor line
      const cursorLine = terminal.lastElementChild;
      terminal.insertBefore(newLine, cursorLine);
    }
    
    // Update status every 5 seconds
    setInterval(updateDeploymentStatus, 5000);
    
    // Blinking cursor animation
    const style = document.createElement('style');
    style.textContent = `
      @keyframes blink {
        0%, 50% { opacity: 1; }
        51%, 100% { opacity: 0; }
      }
    `;
    document.head.appendChild(style);
  </script>
</body>
</html>
